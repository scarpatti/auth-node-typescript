{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Docker launch debugging",
            "port": 9222,
            "restart": true,
            "timeout": 600000,
            "remoteRoot": "/usr/app",

            "preLaunchTask": "tsc-watch"
            // "runtimeExecutable": "docker-compose",
            // "runtimeArgs": ["up"],
        },
    ]
}
