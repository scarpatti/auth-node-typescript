NODE_ENV=test
APP_PORT=3333

DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=changeme
DB_DATABASE=auth

REDIS_DB_PORT=

PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin
PGADMIN_PORT=5050

JWT_KEY=d41d8cd98f00b204e9800998ecf8427e
JWT_ACCESS_TOKEN_EXPIRED=360s
JWT_REFRESH_TOKEN_EXPIRED=3000s
JWT_REDIS_KEY_NAME=bl_


# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL = "postgresql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_DATABASE}?connect_timeout=3000&pool_timeout=3000&socket_timeout=3000"

